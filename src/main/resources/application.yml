---
server:
  port: 8888
security:
  basic:
    enabled: true 
  user:
    name: vind
    password: vind
management:
  security:
    enabled: false
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/out-sight/config-repo
          search-paths:
            - service*